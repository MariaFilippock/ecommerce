export interface ItemType {
    id: number;
    title: string;
    img: string;
    desc: string;
    category: string;
    price: string;
}

export interface ItemAtCartType {
    id: number;
    count: number;
}
